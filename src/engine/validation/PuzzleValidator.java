package engine.validation;

import engine.Puzzle;

/**
 * @author Nerijus
 */
public class PuzzleValidator {

    /**
     * Checks if puzzle contains valid questions
     * and that answers can fit into the puzzle.
     * Does not check if puzzle can be solved or not.
     *
     * @param puzzle puzzle to validate
     * @return true if puzzle is valid
     */
    public static boolean isValid(Puzzle puzzle) {
        // TODO: validate
        return true;
    }
}
